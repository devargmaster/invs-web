services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: invs_web
    restart: unless-stopped
    ports:
      - '5174:5174'
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
